<% if @updated %>
  $("#item-<%= @item.id %> .item-name").html("<%= @item.name %>");
  alertify.success('Item updated successfully');
<% else %>
  alertify.error("<%= @item.errors.full_messages.first %>");
<% end %>