<% if @list.persisted? %>
  $('#create-list-modal').modal('hide');
  $('#list_name').val('');
  $('#my-own-lists').prepend('<%=j render(@list) %>');
  alertify.success('created list');
<% else %>
<% end %>