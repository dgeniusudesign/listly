<% if @list.persisted? %>
  $('#create-list-modal').modal('hide');
  $('#list_name').val('');
  $('#my-own-lists').prepend('<%=j render(@list) %>');
  alertify.success('Created new list');
<% else %>
  alertify.error("<%= @list.errors.full_messages.first %>");
<% end %>